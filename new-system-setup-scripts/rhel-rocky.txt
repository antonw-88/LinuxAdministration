#Add minimize etc to windows:
gsettings set org.gnome.desktop.wm.preferences button-layout ':minimize,maximize,close'

#Grant sudo-privileges to user acc: 
usermod -aG sudo username

#Install important programs:
htop
tripwire
wireshark
lynis
tlp - power managment


